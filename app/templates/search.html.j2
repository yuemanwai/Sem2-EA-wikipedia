{% extends "base2.html.j2" %}
{% import 'bootstrap/wtf.html' as wtf %}
{% block app_content %}
{# 可以用Whoosh / Elasticsearch 做search engine #}
<p>
<form class="d-flex-inliine" action="{{ url_for('search') }}" role="search" style="display: inline-flex;width: 800px;">
    <input class="form-control me-2 " type="search" placeholder="{{ keyword }}" aria-label="Search" name="q">
    <button class="btn btn-outline-success" type="submit">{{_('Search') }}</button>
</form>

{% endblock %}