<table class="table table-hover">
    <tr>
        <td>
            {{ _('From Wikipedia, the free encyclopedia') }}<br/>
            {% if post %}
            {{ post.article_content }}
            {% else %}
            <h3>&emsp;&emsp;<b>{{ _("The article that you're looking for doesn't exist.") }}</b></h3>
            {% endif %}
        </td>
    </tr>
</table>
</div>